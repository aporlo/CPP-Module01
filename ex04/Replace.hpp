#ifndef REPLACE_H
# define REPLACE_H

#include <iostream>
#include <string>
#include <fstream>

Class Myfile
{
    public:

        void    Myfile( void );
        void    ~Myfile( void );

    private:
}
